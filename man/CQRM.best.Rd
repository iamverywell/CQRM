% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CQRM.R
\name{CQRM.best}
\alias{CQRM.best}
\title{Single-quantile MinP test (best-tau selection)}
\usage{
CQRM.best(
  count1,
  count2,
  tau_vec = c(0.2, 0.4, 0.6, 0.8),
  B = 10000,
  seed = 12345,
  M = 50
)
}
\arguments{
\item{count1}{Gene \eqn{\times} sample integer count matrix, group 1.
Genes in rows, samples in columns.}

\item{count2}{Gene \eqn{\times} sample integer count matrix, group 2.
Genes in rows, samples in columns.}

\item{tau_vec}{Numeric vector of quantile levels to aggregate.
Default \code{c(0.2, 0.4, 0.6, 0.8)}.}

\item{B}{Integer. Number of draws from the null distribution for
permutation calibration. Default \code{10000}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{12345}.}

\item{M}{Integer. Number of jitter replicates per gene per quantile.
Default \code{50}.}
}
\value{
Numeric vector of length \code{nrow(count1)} containing one
  calibrated p-value per gene.
}
\description{
Tests each quantile in \code{tau_vec} independently (one chi-squared test
per quantile, scalar variance) and takes the minimum p-value across
quantiles. The null distribution is generated analytically from independent
normals, making this faster than \code{\link{CQRM}}.
}
\examples{
\dontrun{
set.seed(1)
G  <- 100; n <- 10
count1 <- matrix(rnbinom(G * n, mu = 5,  size = 0.5), G, n)
count2 <- matrix(rnbinom(G * n, mu = 10, size = 0.5), G, n)
pv <- CQRM.best(count1, count2, B = 500, M = 10)
}

}
